// Single source of truth for which Flare deployment the extension talks to.
// Swap this (and the matching host in manifest.json's host_permissions) when
// building for production — see README.md.
export const APP_URL = 'http://localhost:3000'
