{
  "name": "@flare/extension",
  "version": "0.1.0",
  "private": true,
  "description": "Flare Chrome extension — capture URLs and selected text into your Flare Vault.",
  "scripts": {
    "build": "node scripts/build.js",
    "generate-icons": "node scripts/generate-icons.js"
  }
}
